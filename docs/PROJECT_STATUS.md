# 랜덤 룰렛 메이커 — 프로젝트 현황 기획서

> 최종 업데이트: 2026-02-27
> 버전: 1.0.0+1
> Flutter SDK: ^3.10.4

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 앱 이름 | 랜덤 룰렛 메이커 (RandomRouletteMaker) |
| 목표 | 5초 만에 만드는 빠른 의사결정 룰렛 앱 |
| 타겟 유저 | 20-30대 일반인, 교사, 콘텐츠 크리에이터 |
| 핵심 가치 | 속도(빠른 생성) + 유연성(커스터마이즈) + 재미(애니메이션/사운드) |
| 상태관리 | ChangeNotifier (Bloc/Riverpod 금지) |
| 저장소 | SharedPreferences (로컬 전용, 서버 없음) |
| 라우팅 | Named routes (AppRoutes 상수) |
| 디자인 시스템 | Electric Navy (다크 베이스 + 시안 악센트 #00D4FF) |

---

## 2. 지원 플랫폼

| 플랫폼 | 상태 | 비고 |
|--------|------|------|
| Android | ✅ 지원 | API 24+ (Android 7.0) |
| iOS | ✅ 지원 | iOS 13+ |
| Web | ✅ 지원 | Chrome 디버그 확인 완료 |
| Windows | ✅ 지원 | 데스크톱 빌드 가능 |

---

## 3. 화면 구성 (7개 + 1탭)

### 3.1 스플래시 (`/splash`)
- 앱 초기화 (LocalStorage, PremiumService, AdService)
- 전체 화면 스플래시 이미지 + 하단 로딩 인디케이터
- 1초 최소 딜레이 후 → 홈 화면 이동

### 3.2 홈 (`/home`)
- **퀵 런치 카드**: 마지막 사용 룰렛 바로 실행
- **내 세트 목록**: 룰렛 CRUD (생성/수정/복제/삭제/이름변경)
- **추천 스타터**: 템플릿 빠른 접근 (4개 칩)
- **사용량 배지**: "2/3" (무료) 또는 "2/∞" (프리미엄)
- **하단 탭바**: 룰렛 | 코인 | 주사위 | 숫자 (4탭)

### 3.3 에디터 (`/editor`)
- 룰렛 이름 입력 (최대 30자)
- 아이템 리스트 (ReorderableListView, 드래그 정렬)
- 아이템별 라벨(최대 20자) + 색상 자동 배정 + 가중치(1-10)
- 검증: 최소 2개 아이템 필요
- 진입 모드: 신규 / 편집 / 템플릿에서 복사

### 3.4 플레이 (`/play`)
- **룰렛 휠**: CustomPainter 기반 렌더링
- **스핀 애니메이션**: 물리 기반 easeOut (2.5~7초)
- **스핀 모드**: 일반 / 중복 제거 세그먼트 탭
- **결과 모달**: 당첨 아이템 표시
- **히스토리**: BottomSheet로 과거 결과 조회
- **통계**: 아이템별 당첨 횟수/비율

### 3.5 템플릿 (`/templates`)
- 7개 기본 스타터 세트
- 상세 BottomSheet (아이템 미리보기)
- 원탭 → 에디터로 복사

### 3.6 설정 (`/settings`)
- **배경 프리셋**: Atmosphere 10개 (무료 2 + 프리미엄 8)
- **앱 테마**: 6개 팔레트 (무료 2 + 프리미엄 4)
- **휠 테마**: 8개 스타일 (무료 2 + 프리미엄 6)
- **언어**: 7개 지원 + 시스템 기본값
- **사운드/햅틱/스핀 시간**: 설정 옵션

### 3.7 페이월 (`/paywall`)
- 프리미엄 기능 소개 + 구매 유도
- 현재 Mock 구현 (Phase 6에서 실제 IAP 연동)

### 3.8 도구 탭 (홈 탭 내)
- 코인 플립 / 주사위 굴리기 / 랜덤 숫자 생성기

---

## 4. 도메인 모델

### 4.1 Roulette (룰렛 세트)
```
id          : String (UUID v4)
name        : String (1-30자)
items       : List<Item> (최소 2개)
createdAt   : DateTime
updatedAt   : DateTime
lastPlayedAt: DateTime?
```

### 4.2 Item (아이템)
```
id         : String (UUID v4)
label      : String (1-20자)
colorValue : int (ARGB, 예: 0xFFE57373)
order      : int (순서 인덱스)
weight     : int (1-10, 기본 1, 가중치)
```

### 4.3 History (결과 히스토리)
```
id              : String (UUID v4)
rouletteId      : String (FK → Roulette)
resultLabel     : String (당첨 아이템 라벨 스냅샷)
resultColorValue: int (당첨 아이템 색상 스냅샷)
playedAt        : DateTime
```
- 룰렛당 최대 **20개** (FIFO 자동 삭제)

### 4.4 Settings (설정)
```
soundEnabled   : bool (기본 true)
soundPack      : enum {basic, clicky, party}
hapticStrength : enum {off, light, strong}
spinDuration   : enum {short=2.5초, normal=4.5초, long=7초}
themeId        : String (기본 'indigo')
wheelThemeId   : String (기본 'classic')
atmosphereId   : String (기본 'deep_space')
localeCode     : String (기본 'system')
lastUsedRouletteId: String?
```
- 스키마 버전 관리: v1→v2 마이그레이션 지원

### 4.5 PremiumState (프리미엄 상태)
```
isPremium   : bool
purchaseDate: DateTime?
```

---

## 5. 프리미엄/무료 구분

### 5.1 무료 플랜 제한

| 항목 | 무료 | 프리미엄 |
|------|------|----------|
| 룰렛 세트 수 | 최대 3개 | 무제한 (∞) |
| 히스토리 | 룰렛당 20개 | 룰렛당 20개 |
| 아이템 수 | 최대 50개 | 최대 50개 |
| 배경 프리셋 | 2개 (deep_space, charcoal) | 10개 전체 |
| 앱 테마 | 2개 (indigo, emerald) | 6개 전체 |
| 휠 테마 | 2개 (classic, candy) | 8개 전체 |
| 광고 | 있음 | 없음 |

### 5.2 프리미엄 잠금 콘텐츠

**배경 프리셋 (8개 잠김)**
aurora, sunset_glow, ocean_depth, neon_city, forest_night, rose_gold, starfield, lava

**앱 테마 (4개 잠김)**
Sunset, Ocean, Rose, Violet

**휠 테마 (6개 잠김)**
neon, midnight, forest, coral, galaxy, gold

---

## 6. 다국어 지원

| 코드 | 언어 | 번역 상태 |
|------|------|-----------|
| `en` | English | ✅ 완료 (기준 언어) |
| `ko` | 한국어 | ✅ 완료 |
| `es` | Español | ⚠️ 31개 미번역 |
| `ja` | 日本語 | ⚠️ 32개 미번역 |
| `pt` | Português | ⚠️ 31개 미번역 |
| `pt_BR` | Português (BR) | ⚠️ 47개 미번역 |
| `zh` | 中文 | ⚠️ 31개 미번역 |
| `zh_Hans` | 简体中文 | ⚠️ 47개 미번역 |

---

## 7. 기본 템플릿 (7개)

| # | 이름 | 아이콘 | 아이템 수 |
|---|------|--------|-----------|
| 1 | Yes / No | ✅ | 10개 |
| 2 | 진실 혹은 도전 | 🎭 | 10개 |
| 3 | 팀 나누기 | 👥 | 8개 |
| 4 | 숫자 (1-10) | 🔢 | 10개 |
| 5 | 음식 고르기 | 🍕 | 5개 |
| 6 | 랜덤 순서 / 플레이어 | 🎲 | 8개 |
| 7 | 우승자 뽑기 | 🏆 | 8개 |

---

## 8. 아키텍처

### 8.1 레이어 구조
```
lib/
├── core/          ← 디자인 토큰, 상수, 유틸, 공통 위젯
├── domain/        ← 불변 모델 + JSON 직렬화
├── data/          ← Repository + Service (싱글턴)
├── features/      ← 화면별 UI + State (ChangeNotifier)
└── l10n/          ← 다국어 리소스 (ARB → 자동 생성)
```

### 8.2 데이터 흐름
```
UI (setState / ValueListenableBuilder)
  ↓
Notifier (ChangeNotifier.notifyListeners)
  ↓
Repository (CRUD 로직)
  ↓
LocalStorage (SharedPreferences 래퍼)
```

### 8.3 주요 싱글턴
| 클래스 | 역할 |
|--------|------|
| `LocalStorage.instance` | SharedPreferences 래퍼 |
| `RouletteRepository.instance` | 룰렛 CRUD + 히스토리 |
| `SettingsNotifier.instance` | 설정 상태 관리 |
| `HomeNotifier.instance` | 홈 화면 상태 관리 |
| `PremiumService` (static) | 프리미엄 상태 + 제한 체크 |
| `AdService` (static) | 광고 서비스 (플랫폼별 분기) |

### 8.4 라우팅
```dart
/splash    → SplashScreen (초기화)
/home      → HomeScreen (메인)
/editor    → EditorScreen (생성/편집)
/play      → PlayScreen (스핀)
/templates → TemplatesScreen (템플릿)
/settings  → SettingsScreen (설정)
/paywall   → PaywallScreen (프리미엄)
```
- SmoothPageRoute: 슬라이드(400ms) + 페이드 전환

---

## 9. 디자인 시스템 — Electric Navy

### 9.1 색상 팔레트
| 토큰 | 값 | 용도 |
|------|-----|------|
| `bgBase` | `#070B14` | 앱 배경 최하단 |
| `bgElevated` | — | 부유 요소 배경 |
| `bgCard` | `#0E1628` | 일반 카드 |
| `bgHero` | `#0F1C30` | 히어로/퀵런치 카드 |
| `accent` | `#00D4FF` | 시안 주 악센트 |
| `accentDeep` | `#0284C7` | 그래디언트 시작점 |
| `spark` | `#7B61FF` | 보라 보조 악센트 |
| `stopRed` | `#FF4466` | 정지/삭제 빨강 |
| `textPrimary` | `#FFFFFF` | 주 텍스트 |
| `textSecondary` | `50% white` | 보조 텍스트 |
| `textTertiary` | `22% white` | 비활성 텍스트 |

### 9.2 주요 그래디언트
- **SPIN 버튼**: `[#0284C7 → #00D4FF]` + 글로우 `#00D4FF@40%`
- **STOP 버튼**: `[#CC2255 → #FF4466]` + 글로우 `#FF4466@40%`
- **골드 (포인터)**: `[#FFB800 → #FFD54F]` + 글로우 `#FFB800@40%`

### 9.3 디멘션
| 토큰 | 값 |
|------|-----|
| `cardRadius` | 15px |
| `buttonRadius` | 100px (완전 둥근) |
| `buttonHeight` | 56px |
| `padding` | 16px |

---

## 10. 패키지 의존성

### 10.1 프로덕션
| 패키지 | 버전 | 용도 |
|--------|------|------|
| `shared_preferences` | ^2.3.0 | 로컬 저장소 |
| `google_fonts` | ^6.1.0 | 타이포그래피 (Nunito, Syne) |
| `flutter_animate` | ^4.5.0 | 애니메이션 (예정) |
| `lottie` | ^2.4.0 | Lottie 애니메이션 (예정) |
| `share_plus` | ^10.1.0 | 결과 공유 |
| `google_mobile_ads` | ^5.1.0 | 광고 (모바일) |

### 10.2 개발
| 패키지 | 버전 | 용도 |
|--------|------|------|
| `flutter_lints` | ^6.0.0 | 린트 규칙 |
| `flutter_launcher_icons` | ^0.14.2 | 앱 아이콘 생성 |
| `flutter_native_splash` | ^2.4.4 | 네이티브 스플래시 |

---

## 11. 개발 로드맵 & 현황

### ✅ 완료 단계

| Phase | 내용 | 상태 |
|-------|------|------|
| **Phase 1** | 프로젝트 구조 + 보일러플레이트 | ✅ 완료 |
| **Phase 2** | 코어 CRUD + 히스토리 | ✅ 완료 |
| **Phase 3** | 홈 UI 고도화 | ✅ 완료 |
| **Phase 4** | 프리미엄 + 광고 서비스 스텁 | ✅ 완료 |
| **Phase 5** | 플레이/도구 탭 UI 하이글로스 | ✅ 완료 |
| **Phase 5.5** | Sharp Arcade 디자인 (블러 제거, 클린 엣지) | ✅ 완료 |

### ⏳ 진행중

| Phase | 내용 | 상태 | 상세 |
|-------|------|------|------|
| **Phase 5.7** | Atmosphere 배경 프리셋 | ⏳ 진행중 | 모델 정의 완료, AppBackground 렌더링 미적용, isDark 분기 정리 잔여 |

#### Phase 5.7 상세 현황
- [x] AtmospherePreset 모델 생성
- [x] 10개 프리셋 정의 (Free 2 + Premium 8)
- [x] Settings 모델에 `atmosphereId` 필드 추가
- [x] PremiumService.canUseAtmosphere() 추가
- [ ] AppBackground → 선택된 Atmosphere 그래디언트 렌더링
- [ ] isDark 분기 전부 제거 (다크 단일 모드)
- [ ] Settings 화면: Atmosphere 2열 그리드 추가
- [ ] 카드/네비바/바텀시트 글래스모피즘 적용

### 📋 예정 단계

| Phase | 내용 | 주요 작업 |
|-------|------|-----------|
| **Phase 6** | 실서비스 연동 | `in_app_purchase` 실제 IAP, `audioplayers` 사운드, `vibration` 햅틱, 결과 클립보드 복사 |
| **Phase 7** | QA + 스토어 출시 | 유닛/위젯 테스트, 접근성(WCAG AA), 성능 프로파일링(60fps), 스토어 메타데이터 |

---

## 12. 파일 구조 전체

```
lib/
├── main.dart                                    # 앱 진입점
├── app.dart                                     # MaterialApp + 라우팅 + 테마
│
├── core/
│   ├── constants.dart                           # 라우트, 제한값, 색상, 설정 상수
│   ├── design_tokens.dart                       # AppColors, AppDimens, AppShadows
│   ├── app_themes.dart                          # 6개 앱 팔레트 + ThemeData 빌더
│   ├── atmosphere_presets.dart                   # 10개 배경 프리셋
│   ├── roulette_wheel_themes.dart               # 8개 휠 렌더링 스타일
│   ├── utils.dart                               # ID 생성, 색상 헬퍼, 날짜 포맷
│   └── widgets/
│       ├── app_background.dart                  # 레이어드 다크 배경
│       ├── glass_card.dart                      # 글래스모피즘 카드
│       ├── gradient_button.dart                 # 그래디언트 버튼
│       └── section_label.dart                   # 섹션 헤더
│
├── domain/
│   ├── roulette.dart                            # Roulette 모델 + JSON
│   ├── item.dart                                # Item 모델 + JSON
│   ├── history.dart                             # History 모델 + JSON
│   ├── settings.dart                            # Settings 모델 + enum
│   ├── premium_state.dart                       # PremiumState 모델
│   ├── purchase_provider.dart                   # 추상 IAP 인터페이스
│   └── roulette_wheel_theme.dart                # WheelStyle enum + 테마 모델
│
├── data/
│   ├── local_storage.dart                       # SharedPreferences 래퍼
│   ├── roulette_repository.dart                 # 룰렛 CRUD + 히스토리
│   ├── settings_repository.dart                 # 설정 저장/로드
│   ├── premium_service.dart                     # 프리미엄 상태 + 제한
│   ├── mock_purchase_provider.dart              # Mock IAP (Phase 6 교체)
│   ├── ad_service.dart                          # 광고 서비스 진입점
│   ├── ad_service_mobile.dart                   # Google Mobile Ads
│   ├── ad_service_web.dart                      # Web 스텁 (no-op)
│   └── templates_data.dart                      # 7개 기본 템플릿
│
├── features/
│   ├── splash/
│   │   ├── ui/splash_screen.dart                # 초기화 + 스플래시 이미지
│   │   └── state/splash_notifier.dart           # 앱 초기화 로직
│   ├── home/
│   │   ├── ui/home_screen.dart                  # 메인 홈 (탭바, 퀵런치, 세트목록)
│   │   ├── state/home_notifier.dart             # 룰렛 목록 상태
│   │   └── widgets/roulette_card.dart           # 세트 카드 위젯
│   ├── editor/
│   │   ├── ui/editor_screen.dart                # 룰렛 생성/편집 폼
│   │   ├── state/editor_notifier.dart           # 폼 상태 관리
│   │   └── widgets/item_list_tile.dart          # 아이템 행 위젯
│   ├── play/
│   │   ├── ui/play_screen.dart                  # 스핀 화면
│   │   ├── state/play_notifier.dart             # 스핀 애니메이션 상태
│   │   ├── widgets/roulette_wheel.dart          # 휠 CustomPainter + 포인터
│   │   └── widgets/stats_sheet.dart             # 통계 바텀시트
│   ├── templates/
│   │   ├── ui/templates_screen.dart             # 템플릿 목록
│   │   └── widgets/template_card.dart           # 템플릿 카드
│   ├── settings/
│   │   ├── ui/settings_screen.dart              # 설정 화면
│   │   ├── state/settings_notifier.dart         # 설정 상태 (싱글턴)
│   │   └── widgets/premium_demo_widget.dart     # 프리미엄 데모
│   ├── paywall/
│   │   └── ui/paywall_screen.dart               # 프리미엄 구매
│   └── tools/
│       └── tools_tab.dart                       # 코인/주사위/숫자 도구
│
└── l10n/
    ├── app_en.arb                               # 영어 (기준)
    ├── app_ko.arb                               # 한국어
    ├── app_es.arb / app_ja.arb / ...            # 기타 언어
    └── app_localizations*.dart                  # 자동 생성 클래스

docs/
├── PRD.md                                       # 제품 요구사항
├── UX_FLOW.md                                   # 사용자 흐름
├── DATA_MODEL.md                                # 데이터 모델 스펙
├── ROADMAP.md                                   # 개발 로드맵
├── DESIGN_DIRECTION.md                          # 디자인 방향 (Phase 5.7+)
├── QA_CHECKLIST.md                              # QA 테스트 케이스
└── PROJECT_STATUS.md                            # ← 이 문서

assets/icons/
├── app_icon.png                                 # 앱 아이콘
└── splash_image.png                             # 스플래시 이미지
```

---

## 13. 알려진 이슈 & 기술 부채

| # | 항목 | 심각도 | 비고 |
|---|------|--------|------|
| 1 | 다국어 미번역 (es, ja, pt, pt_BR, zh, zh_Hans) | 중 | 31~47개 키 미번역 |
| 2 | 테스트 코드 없음 | 중 | Phase 7에서 추가 예정 |
| 3 | MockPurchaseProvider 사용 중 | 중 | Phase 6에서 실제 IAP 교체 |
| 4 | 사운드/햅틱 미구현 | 저 | 설정 UI만 존재, 실제 동작 없음 |
| 5 | `Color.value` deprecated 경고 (2곳) | 저 | `item_list_tile.dart:252` |
| 6 | `use_build_context_synchronously` 경고 (1곳) | 저 | `roulette_card.dart:215` |
| 7 | flutter_analyze 10개 info 잔존 | 저 | 기능 영향 없음 |

---

## 14. 핵심 제약사항 (CLAUDE.md)

- **Bloc/Riverpod 사용 금지** → ChangeNotifier만 사용
- **Color.value 금지** → `Color.toARGB32()` 사용
- **AnimatedBuilder 콜백**: `(_, _)` 패턴
- **async gap 후 BuildContext**: 미리 `Navigator.of(context)` 캐시
- **const Semantics 금지**: const constructor 아님
